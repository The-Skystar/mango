# 应用服务 WEB 访问端口
server:
  port: 2021
spring:
  application:
    name: mango-oauth2-auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        refresh: true