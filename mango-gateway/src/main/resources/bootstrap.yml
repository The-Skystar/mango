# 应用服务 WEB 访问端口
server:
  port: 3000
spring:
  application:
    name: mango-gateway
  profiles:
    active: peer1
  cloud:
    nacos:
      config:
        server-addr: 39.97.181.177:8848
        file-extension: yaml
        refresh: true